services:
  serverscheduler:
    build: .
    image: serverscheduler:latest
    env_file: .env
    ports:
      - "80:8080"
    environment:
      - DB_PATH=/app/data/serverscheduler.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
